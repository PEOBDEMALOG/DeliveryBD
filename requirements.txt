# requirements.txt — PEO-BD  |  pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.32        # upload de arquivos no FastAPI

# ── Banco de dados ─────────────────────────────────────────
sqlalchemy==2.0.49
aiosqlite==0.22.1               # SQLite async (desenvolvimento local)
asyncpg==0.29.0                 # PostgreSQL async (Vercel / produção) — 0.30.0 remove channel_binding
greenlet                        # exigido pelo SQLAlchemy async

# ── Processamento de dados ─────────────────────────────────
pandas==3.0.3
openpyxl==3.1.5                 # leitura/escrita xlsx
xlrd==2.0.2                     # leitura xls legado

# ── Utilitários ────────────────────────────────────────────
python-dotenv==1.2.2            # variáveis de ambiente (.env)
httpx==0.28.1                   # requisições HTTP async
aiofiles==25.1.0                # arquivos estáticos async
reportlab==4.2.0                # geração de PDF
resend>=2.0.0                   # e-mail transacional para alertas internos Emalog
anthropic>=0.40.0                # Assistente de Diagnóstico (chat contextual por tipo de erro)

# ── Autenticação ───────────────────────────────────────────
# 3.5.0 em vez do 3.4.0 pedido originalmente (que corrige os CVEs
# CVE-2024-33663/33664): 3.4.0 declara "pyasn1<0.5.0", o que forçaria
# downgrade do pyasn1 pra uma versão com 2 CVEs de DoS conhecidas
# (GHSA-jr27-m4p2-rc6r, GHSA-63vm-454h-vhhq) — confirmado com
# `pip install -r requirements.txt` limpo, o resolvedor rejeita a
# combinação 3.4.0 + pyasn1 corrigido (ResolutionImpossible). 3.5.0
# relaxa pra "pyasn1>=0.5.0" e resolve limpo pro pyasn1 mais recente,
# sem esse efeito colateral — mesma correção dos CVEs, sem regressão.
python-jose[cryptography]==3.5.0  # JWT (assinatura/verificação HS256)
pyasn1==0.6.4                     # pin explícito — CVE-2026-30922/23490 corrigidos em 0.6.3+
passlib[bcrypt]==1.7.4            # hashing de senha (reservado para uso futuro)
